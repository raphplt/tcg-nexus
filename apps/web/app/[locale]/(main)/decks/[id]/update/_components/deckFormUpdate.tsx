import { useTranslations } from "next-intl";
import React from "react";
import { DeckFormProps } from "@/types/formDeck";

/**
 * Component for updating an existing deck.
 *
 * @param props Component properties containing deck format options.
 * @returns JSX Element for deck update form.
 */
const DeckFormUpdate: React.FC<DeckFormProps> = (_props: DeckFormProps) => {
  return <div>deckFormUpdate</div>;
};

export default DeckFormUpdate;
